{
    "version": 2,
    "builds": [
        {
            "src": "src/main.serverless.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/main.serverless.ts"
        }
    ]
}